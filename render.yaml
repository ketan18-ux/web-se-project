services:
  - type: web
    name: govportal-backend
    env: node
    buildCommand: npm install && npm run build && npm start
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
